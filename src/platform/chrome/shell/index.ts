// @mkbabb/atlas · chrome/shell — the platform shell barrel (src-rearchitecture §A.2; O-B8a).
// The one app frame the router mounts under: PlatformShell hosts the Dock instrument + the routed
// view. (Split from the flat platform/chrome/ bag; render-identity — a pure structural re-home.)
export { default as PlatformShell } from "./PlatformShell.vue";
